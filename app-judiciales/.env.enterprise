# Variables de entorno para el Sistema de Expedientes Militares
# CONFIGURACIÓN PARA PRODUCCIÓN EMPRESARIAL CON ALTA DISPONIBILIDAD

# MongoDB Configuration (Cloud Database - usar tu URI real)
MONGODB_URI=mongodb+srv://bs00001:l83F7fPkVncfmL9p@bacsystem.66ilhtt.mongodb.net/?appName=bacsystem
MONGODB_DATABASE=expedientes_db

# Backend Configuration (Producción Empresarial)
JWT_SECRET=ultra_secure_production_jwt_secret_key_enterprise_2024_change_immediately
JWT_EXPIRATION=12h
JWT_REFRESH_EXPIRATION=72h
GIN_MODE=release
ENV=production
PORT=8082

# Frontend Configuration
NODE_ENV=production
BACKEND_URL=http://backend:8082
NEXT_TELEMETRY_DISABLED=1

# CORS Configuration (permitir acceso desde cualquier IP en producción empresarial)
CORS_ALLOWED_ORIGINS=*

# Upload Configuration (más restrictivo en empresarial)
MAX_UPLOAD_SIZE=5242880
UPLOAD_PATH=/app/uploads

# Email Configuration (configurar para producción empresarial)
EMAIL_HOST=smtp.empresa.mil
EMAIL_PORT=587
EMAIL_USERNAME=sistema@empresa.mil
EMAIL_PASSWORD=enterprise_email_password_2024

# Timezone
TZ=America/Lima

# Logging (detallado para empresarial)
LOG_LEVEL=warn
LOG_FORMAT=json

# Security & Rate Limiting (más restrictivo para empresarial)
RATE_LIMIT_REQUESTS=200
RATE_LIMIT_WINDOW=3600

# Database Connection Pool (para alta carga)
DB_MAX_CONNECTIONS=100
DB_IDLE_TIMEOUT=30s

# Performance Settings
CACHE_TTL=3600
SESSION_TIMEOUT=1800